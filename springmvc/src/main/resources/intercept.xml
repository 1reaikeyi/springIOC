<?xml version="1.0" encoding="UTF-8"?>
<!-- 根标签：Spring IoC容器的入口 -->
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/beans" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 此处配置Bean、依赖注入等内容 -->
    <context:component-scan base-package="HandlerInterceptor"/>
    <mvc:annotation-driven/>
    <!-- Thymeleaf视图解析器配置 -->
    <bean id="thymeleafViewResolver" class="org.thymeleaf.spring6.view.ThymeleafViewResolver">
        <!-- 设置字符编码为UTF-8，确保中文等特殊字符正确显示 -->
        <property name="characterEncoding" value="UTF-8"/>
        <!-- 设置视图解析器的优先级，数值越小优先级越高 -->
        <property name="order" value="1"/>
        <!-- 配置模板引擎 -->
        <property name="templateEngine">
            <bean class="org.thymeleaf.spring6.SpringTemplateEngine">
                <property name="templateResolver">
                    <bean class="org.thymeleaf.spring6.templateresolver.SpringResourceTemplateResolver">
                        <property name="prefix" value="classpath:/intercept/"/>
                        <property name="suffix" value=".html"/>
                        <property name="templateMode" value="HTML"/>
                        <property name="characterEncoding" value="UTF-8"/>
                    </bean>
                </property>
            </bean>
        </property>
    </bean>
    <!-- 配置拦截器 -->
    <mvc:interceptors>
<!--        <mvc:interceptor>-->
<!--            <mvc:mapping path="/**"/>-->
<!--            &lt;!&ndash; 排除路径 &ndash;&gt;-->
<!--            <mvc:exclude-mapping path="/test"/>-->
<!--            <bean class="intercept.InterceptTest"/>-->
<!--        </mvc:interceptor>-->
    <!--全局拦截器-->
        <bean class="HandlerInterceptor.Intercept1"/>
        <bean class="HandlerInterceptor.Intercept2"/>
        <bean class="HandlerInterceptor.Intercept3"/>
    </mvc:interceptors>
    <mvc:view-controller path="/test" view-name="index"/>
</beans>