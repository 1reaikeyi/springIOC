<?xml version="1.0" encoding="UTF-8"?>
<!-- 根标签：Spring IoC容器的入口 -->
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="http://www.springframework.org/schema/beans" xmlns:tool="http://www.springframework.org/schema/tool"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/tool http://www.springframework.org/schema/tool/spring-tool.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 此处配置Bean、依赖注入等内容 -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>
        <property name="url" value="jdbc:mysql://localhost:3306/tostu"/>
        <property name="username" value="root"/>
        <property name="password" value="111222"/>
    </bean>
     <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
<!--         配置数据源，引用之前定义的dataSource Bean -->
        <property name="dataSource" ref="dataSource"/>
<!--         配置MyBatis核心配置文件路径 -->
        <property name="configLocation" value="mybatis/mybatis.xml"/>
<!--         配置类型别名包，自动扫描并注册实体类 -->
         <property name="typeAliasesPackage" value="bmybatis"/>
<!--         配置Mapper XML文件路径，自动扫描并注册Mapper接口 -->
         <property name="mapperLocations" value="mybatis/account.xml"/>
    </bean>
<!--    配置Mapper扫描器，自动扫描并注册Mapper接口 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="bmybatis"/>
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
    </bean>

    <context:component-scan base-package="bmybatis"/>
<!--    开启事务注解驱动 -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <!-- 配置事务管理器，引用之前定义的dataSource Bean -->
        <property name="dataSource" ref="dataSource"/>
    </bean>
    <!-- 开启事务注解驱动，引用之前定义的transactionManager Bean -->
    <tx:annotation-driven transaction-manager="transactionManager"/>


</beans>